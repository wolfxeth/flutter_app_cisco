import 'package:flutter/material.dart';

class WebexScreen extends StatelessWidget {
  const WebexScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Padding(padding: EdgeInsets.all(16), child: Text('Webex content (placeholder)')));
  }
}
